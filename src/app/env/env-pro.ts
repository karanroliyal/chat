export const environment = {
    production: true,
    base_url: `${window.location.protocol}//${window.location.hostname}:4200/`,
    server_url: `${window.location.protocol}//${window.location.hostname}/api/`
  };